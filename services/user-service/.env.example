# Server
PORT=3003
NODE_ENV=development

# Database
DATABASE_URL=postgresql://postgres:your_password_here@localhost:5432/userdb

# JWT (used for verification only - secret must match auth-service)
JWT_SECRET=your_jwt_secret_here